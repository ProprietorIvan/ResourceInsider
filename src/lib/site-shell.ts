/** Responsive horizontal padding (use inside max-width columns or with full bleed) */
export const SITE_PAD_X =
  'px-5 sm:px-8 lg:px-12 xl:px-16 2xl:px-24'

/** Full-bleed row: full width + side padding */
export const SITE_SHELL_X = `w-full max-w-none ${SITE_PAD_X}`
